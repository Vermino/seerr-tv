# Single source of truth for the app version.
# RELEASE RULE: every published APK MUST increase VERSION_CODE by at least 1
# (monotonic integer) or Android refuses to install it over the previous version.
# Bump VERSION_NAME too for humans.
VERSION_NAME=1.5
VERSION_CODE=6
